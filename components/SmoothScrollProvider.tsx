"use client";

/**
 * SmoothScrollProvider — scroll 100% natif navigateur.
 * Aucun JS, aucune lib, aucune boucle RAF.
 * Le scroll-behavior smooth est géré dans globals.css :
 *   html { scroll-behavior: smooth; }
 */
export default function SmoothScrollProvider() {
  return null;
}