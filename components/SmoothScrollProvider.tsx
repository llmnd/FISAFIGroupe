'use client';

// This component is no longer needed - scroll-behavior is handled by CSS reset.css
// The constant reflow and mutation observation was causing jank and scroll conflicts.
export default function SmoothScrollProvider() {
  return null;
}
